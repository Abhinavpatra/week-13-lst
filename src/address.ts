export const PRIVATE_KEY = process.env.PRIVATE_UINT8_ARRAY as string;
export const PUBLIC_KEY = process.env.PUBLIC_KEY as string;
// the public key of the CLI Address using which you generated the token
export const TOKEN_MINT_ADDRESS = process.env.TOKEN_MINT_ADDRESS as string;
// The token mint address shown once, when you created the token using the CLI